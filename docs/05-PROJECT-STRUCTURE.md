# Murmur — Project Structure

The tree is decided here so it is not improvised per feature. A file that does not have an obvious home in this layout is a signal the design is drifting.

---

## 1. Full tree

This is the tree as built, not as imagined. If a file has no obvious home in it,
the design is drifting.

```
murmur/
├── CLAUDE.md                        # the rulebook — read first, every session
├── README.md · LICENSE (MIT)
├── docs/                            # 00–06, this set
├── index.html · pill.html · onboarding.html   # the three Vite entry documents
├── vite.config.ts                   # multi-entry: dashboard, pill, onboarding
├── scripts/sot.mjs                  # the SOURCE OF TRUTH keyword search
│
├── src/                             # ── FRONTEND ──────────────────────────
│   ├── entries/                     # dashboard.tsx · pill.tsx · onboarding.tsx
│   ├── app/
│   │   ├── dashboard/               # Dashboard, Sidebar, ViewHeader, ViewState
│   │   │   ├── stats/               # StatsView + ActivityChart, LanguageBreakdown,
│   │   │   │                        #   LatencyPanel (no chart library — docs/04 §11)
│   │   │   ├── history/             # HistoryView + HistoryRow + use-history
│   │   │   ├── settings/            # SettingsView + DictionaryManager,
│   │   │   │                        #   ModelManager, to-setting-def
│   │   │   ├── use-hash-route.ts    # deep links: #/settings/privacy
│   │   │   └── use-settings.ts      # one subscriber to SettingsChanged
│   │   ├── pill/                    # Pill + StateDot, PillWaveform,
│   │   │                            #   use-elapsed
│   │   └── onboarding/              # Onboarding + Permission/Model/Hotkey steps
│   │
│   ├── components/global/           # reusable anywhere, slot-driven
│   │   ├── glass-panel/ · stat-card/ · data-list/ · setting-control/
│   │   ├── empty-state/ · error-boundary/ · error-surface/
│   │   └── countdown-ring/ · waveform/ · keycap/ · progress-bar/
│   │       mark/ · segmented-control/
│   │
│   ├── lib/
│   │   ├── bindings.ts              # GENERATED by tauri-specta — never edit
│   │   ├── ipc.ts · use-event.ts    # typed calls and event subscriptions
│   │   ├── hotkey.ts                # KeyboardEvent → HotkeyBinding
│   │   ├── motion.ts                # springs read out of the stylesheet
│   │   └── format.ts · icons.ts · utils.ts
│   │
│   └── styles/                      # tokens.css (every value from docs/04)
│                                    # global.css (Tailwind v4 + @theme)
│
└── src-tauri/                       # ── BACKEND ───────────────────────────
    ├── tauri.conf.json · Info.plist # windows, CSP, mic usage string, LSUIElement
    ├── capabilities/default.json    # Tauri v2 ACL — required, or plugins fail
    │
    └── src/
        ├── main.rs · lib.rs         # wiring only, no logic
        ├── bootstrap.rs             # ★ the ONLY file naming a concrete adapter
        ├── tray.rs                  # menu bar item; pill show/hide
        │
        ├── registry/                # ★ SINGLE SOURCE OF TRUTH
        │   ├── mod.rs               #   CAPABILITIES + validate_registry
        │   ├── capability.rs        #   Capability, SettingDef, NavDef, MetricDef
        │   └── keys.rs              #   every setting key as a constant
        │
        ├── ipc/
        │   ├── factory.rs           # ★ validate → preflight → guard → handler
        │   ├── context.rs           #   AppState, Ports, SessionHandle
        │   ├── bindings.rs          #   specta registry + the export test
        │   ├── events.rs            #   typed events pushed to the frontend
        │   └── commands/            #   session · history · settings · stats
        │                            #   dictionary · models · engine · profiles
        │                            #   system
        ├── session/
        │   ├── machine.rs           # ★ the PURE FSM — returns Effects as data
        │   ├── actor.rs             # ★ the single owner; performs the effects
        │   └── settings_view.rs     #   typed per-session settings snapshot
        │
        ├── pipeline/
        │   ├── vad.rs               # speech detection, silence gating
        │   ├── chunker.rs           # 8–15s chunks at silence, 200ms overlap
        │   ├── worker.rs            # dedicated ASR thread + bounded queue
        │   └── assembler.rs         # join segments, de-duplicate seams
        │
        ├── ports/                   # ★ traits only — zero implementation
        │   └── engine · audio · enhancer · injector · models · permissions
        │
        ├── adapters/                # mod.rs is the factory; one impl per port
        │   ├── whisper/             #   engine, params, prompt, blocklist,
        │   │                        #   hallucination, coreml, state_pool,
        │   │                        #   benchmark, live_tests
        │   ├── cpal/                #   source (realtime callback) + resample
        │   ├── macos/               #   injector, permissions, sound
        │   ├── rules/               #   the enhancement rules + text.rs
        │   └── http_models/         #   catalog, download, store
        │
        ├── services/                # ★ pure SQLite. one verb, one table.
        │   └── sessions · settings · dictionary · metrics · stats · profiles
        │
        ├── db/                      # connection (WAL, pragmas) + migrations/
        ├── types/                   # ★ every shared type. specta-derived.
        │   └── session · audio · transcript · engine · settings · metrics
        │      · dictionary · language · numeric
        ├── error/                   # AppError — the ONLY error crossing IPC
        ├── config/paths.rs          # every filesystem location, resolved once
        └── telemetry/               # tracing setup + per-stage latency
```

**Two files carry the architecture.** `registry/mod.rs` declares what the app
HAS; `ipc/factory.rs` is what every command passes THROUGH. A third,
`bootstrap.rs`, is the only place a concrete adapter is named — which is what
makes swapping an engine or adding a platform a change to one file and one
directory.

---

## 2. Where things go — the decision table

| If you are writing…             | It goes in                     | Never in                     |
| ------------------------------- | ------------------------------ | ---------------------------- |
| A new feature's metadata        | `registry/mod.rs`              | scattered constants          |
| Business logic                  | `ipc/commands/` or `pipeline/` | services, components         |
| A SQL query                     | `services/`                    | commands, pipeline           |
| A trait for something swappable | `ports/`                       | adapters                     |
| A third-party integration       | `adapters/<name>/`             | ports, services              |
| A shared type                   | `types/`                       | inline in the module         |
| A component used in 2+ routes   | `components/global/`           | duplicated per route         |
| A component used in 1 route     | `app/<route>/_components/`     | `components/global/`         |
| UI-only state                   | `stores/`                      | React context, prop drilling |
| Domain state                    | Rust, pushed as events         | Zustand                      |
| A color, size, spring           | `styles/tokens.css`            | the component                |

---

## 3. Naming

| Kind                   | Convention          | Example                   |
| ---------------------- | ------------------- | ------------------------- |
| Rust module            | `snake_case`        | `session/machine.rs`      |
| Rust type              | `PascalCase`        | `TranscriptionEngine`     |
| Rust trait method      | `snake_case`        | `transcribe()`            |
| React component file   | `PascalCase.tsx`    | `StatCard.tsx`            |
| React component folder | `kebab-case/`       | `stat-card/`              |
| Hook                   | `use-*.ts`          | `use-session.ts`          |
| Store                  | `*.store.ts`        | `ui.store.ts`             |
| Service                | plural noun         | `sessions.rs`             |
| Migration              | `NNN_verb_noun.sql` | `001_create_sessions.sql` |
| IPC command            | `verb_noun`         | `start_recording`         |
| Event                  | `noun:verb`         | `session:state-changed`   |

---

## 4. Module header — required on every file

Same format as the guardrail boilerplate, because it is what makes `grep -l` work and keeps context cost flat:

```rust
/**
 * SOURCE OF TRUTH KEYWORDS: SessionMachine, SessionState, TransitionError,
 *   CancelPending, Finalizing, transition, can_transition, SessionEvent
 * WHAT:  The finite state machine governing one recording session.
 * WHY:   Recording state lives in exactly one place so illegal states are
 *        unrepresentable and every transition is logged and persisted.
 * WHERE: Owned by session/actor.rs; driven by ipc/commands/session.rs and
 *        the hotkey handler; read by the pill via session:state-changed.
 */
```

5–10 specific keywords. The next agent greps for a symbol, gets a short file list, and reads only those files. Skipping this is what turns a clean codebase into one nobody can navigate.

---

## 5. Hard limits

- **A file over ~400 lines is doing two things.** Split it.
- **A function over ~50 lines** needs a reason.
- **`ports/` contains no logic** — traits and capability structs only.
- **`services/` functions take parameters and return rows.** No branching on business rules, no calling other services.
- **Nothing imports upward.** `services` never imports `ports`; `ports` never imports `adapters`; `adapters` never import `ipc`. CI enforces this — `src-tauri/src/layering.rs` fails `cargo test` on an upward import, and CI runs the suite on every push, so it fails the build rather than being caught in review.

  Three violations exist today and are recorded in `KNOWN_UPWARD_IMPORTS` in that file, with the reason for each. They are one problem in three places: `AppState`, `SessionContext` and the typed event structs all live in `ipc/` and are all needed by the domain, so `session/actor.rs`, `session/delivery.rs` and `adapters/events.rs` reach up for them. The fix is to move those types down to a layer both sides can see. The list is self-cleaning — fixing one fails the suite until its entry is deleted — and it exists so that a fourth violation cannot appear unnoticed.

  This paragraph replaced a claim that CI enforced the rule at a time when there was no CI at all and the rule was already being broken. A guardrail described in a document and absent from the build is worse than an acknowledged gap, because everyone downstream reasons as though it holds.
